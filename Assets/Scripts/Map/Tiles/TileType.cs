namespace Split.Map.Tiles {
    public enum TileType {
        EMPTY = 0,
        BASIC = 1,
        START = 2,
        END = 3,
        BROKEN = 4,
        BUTTON = 5,
        BRIDGE = 6,
        BRIDGE_BROKEN = 7,
        BRIDGE_BUTTON = 8,
    }
}
