using UnityEngine;

namespace Split.Map.Tiles {
    public class BridgeTile : Tile {

        public BridgeTile(Transform tilePrefab, Vector3 tilePosition, MapGenerator parent, MapData mapData) : base(tilePrefab, tilePosition, parent, mapData) {
        }

        


    }
}