using UnityEngine;

namespace Split.Tiles {
    /// <summary>
    /// Tile that marks where the first player spawns
    /// </summary>
    public class StartTile : TileEntity {
        public StartTile(GameObject gameObject, int gridX, int gridY) : base(gameObject, gridX, gridY) {
        }

        //TODO: Unimplemented class
    }
}